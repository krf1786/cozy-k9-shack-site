# Copy this file to .env on the VPS and fill in real values.
# .env is gitignored — never commit real secrets.

# Shared password for the /upload page (Jaclyn's photo uploads).
UPLOAD_PASSWORD=change-me

# Port the Node server listens on. Nginx reverse-proxies to this port.
PORT=3000
